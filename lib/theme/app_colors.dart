import 'package:flutter/material.dart';

class AppColors {
  static const backgroundTop = Color(0xFF101A23);
  static const backgroundBottom = Color(0xFF725A56);

  static const border = Color(0xFFBDB6A8);

  static const connected = Color(0xFF70E1A1);
  static const disconnected = Color(0xFFD96A6A);

  static const panel = Color(0x22000000);

  static const text = Colors.white;
}